server:
  port : 9090
  shutdown: graceful

spring:
  lifecycle:
    timeout-per-shutdown-phase: 20s
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  #    properties:
  #      hibernate:
  #        format_sql: true
  datasource:
    hikari:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3306/shop_toy?serverTimezone=UTC&characterEncoding=UTF-8
      username: root
      password: 1234
  flyway:
    user: root
    password: 1234
    schemas: shop_toy
    url: jdbc:mysql://localhost:3306/shop_toy?serverTimezone=UTC&characterEncoding=UTF-8
    enabled: true

  #  h2:
  #    console:
  #      enabled: true
  #      path: /h2-console

  # Redis
  redis:
    host: localhost
    port: 6379


  # Swagger
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  config: classpath:logback-local.xml
#  level:
#    org:
#      hibernate:
#        type:
#          descriptor:
#            sql: trace

jwt:
  secret: VlwEyVBsYt9V7zq57TejMnVUyzblYcfPQye08f7MGVA9XkHN
